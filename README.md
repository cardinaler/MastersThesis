# Введение
В настоящей работе исследуется одномерная начально-краевая задача теплопроводности на полуоси с граничным условием второго рода. 

Основные этапы исследования:
* С помощью интегрального **косинус-преобразования Фурье** получены разрешающие соотношения для прямой задачи.
* Сформировано **операторное уравнение** для решения обратной задачи.

В репозитории реализованы и сравниваются две подхода к решению обратной задачи:
1. **Классический подход** — сопряжен с поиском минимума функционала оптимизационными градиентными методами.
2. **Обучаемый подход** — базируется на применении нейронных операторов Фурье (Fourier Neural Operators, FNO).

---
# Структура репозитория
```text
MasterThesis/
│
├── dosc/
│   │    
│   ├── theory.pdf                      # Основные моменты и текущие результаты
│   └── theory.tex                      # tex верстка для theory.pdf
│
├── src/
│   │
│   ├── grad_metgod/                    # Градиентный метод решения
│   │   ├── heat_inv_problem.py         # Определение и решение обратной задачи
│   │   ├── isotonic_grad_descent.py    # Градиентный спуск
│   │   └── pava.py                     # Алгоритм pava проекции на монотонные функции
│   │
│   ├── FNO/                            # Метод решения через нейронный оператор Фурье
│   │   └── dataset_generator.py        # Генератор обучающей выборки
│   │
│   └── heat_operator.py                # Тепловой оператор
│
├── main.py                             # Точка входа
└── README.md
```
---

# Постановка задачи
### Прямая задача
Рассмотрим задачу распространения тепла в полуограниченном стержне при наличии внутренних источников, плотность мощности которых описывается функцией $f(x, t)$. Начальное распределение температуры стержня задано и равно $u_0(x)$, а на границе $x = 0$ определен тепловой поток $\gamma(t)$. 
Математическая формулировка задачи имеет следующий вид:

$$
\begin{aligned}
&u_t(x, t)= a^2u_{xx}(x, t) + f(x, t), \quad x > 0, \quad t > 0, \\
&u_x(0, t) = \gamma(t), \quad t > 0, \\
&u(x, 0) = u_0(x), \quad x >  0.
\end{aligned}
$$

### Обратная задача 
Рассмотрим теперь обратную задачу определения источника. Слагаемое $f(x,t) \equiv f(x)$ считается неизвестным. Для нахождения источника фиксируется дополнительная информация, а именно - значение функции $u_t(x, t)$ на правой границе отрезка времени $[0, T]$. Постановка задачи имеет вид.

$$
\begin{aligned}
&u_t(x, t)= a^2u_{xx}(x, t) + f(x), \quad x > 0, \quad t > 0, \\
&u_x(0, t) = \gamma(t), \quad t > 0,\\
&u(x, 0) = u_0(x), \quad x > 0,\\
&u_t(x, T) = u_T(x), \quad x > 0.
\end{aligned}
$$

Функция $u_T(x)$ задана.
Таким образом, зная $\gamma(t), u_0(x), u_T(x)$, требуется восстановить $u(x, t)$ и $f(x)$.

[Дальнейшее исследование задачи см. в docs/theory.md](./docs/theory.pdf)